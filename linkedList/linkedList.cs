using  System;

class Node{

    public int data;
    public Node Next;

    // Constructor to initialize the node with data like init in python
    public Node(int data)
    {
        Data = data;
        Next = null;
    }

    static void Main()
    { Console.WriteLine("je")}

}